{% extends "Base.twig" %}

{% block title %}Logon{% endblock %}

{% block main %}
    <div class="row justify-content-lg-center mb-3">
        <div class="col-lg-6">
        {% if loginErrors.global %}
            <div class="alert alert-warning" role="alert">
                {{ loginErrors.global }}
            </div>
        {% endif %}

        {% include "Includes/Logon-form.twig" %}
        </div>
    </div>
{% endblock %}