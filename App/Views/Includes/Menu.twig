
<ul class="navbar-nav mr-auto">
    <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
    </li>
    {% for cat, url in navigation %}
        <li class="nav-item">
            <a class="nav-link" href="{{ url }}">{{ cat }}</a>
        </li>
    {% endfor %}
    <li class="nav-item">
        <a class="nav-link" href="/all-pages">All Pages</a>
    </li>
</ul>
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#logonModal">Sign In</button>
